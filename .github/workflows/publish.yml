name: Publish package
on:
    release:
        types: [ created ]

jobs:
    publish:
        runs-on: ubuntu-latest
        steps:
            -   name: Dump GitHub context
                env:
                    GITHUB_CONTEXT: ${{ toJson(github) }}
                run: echo "$GITHUB_CONTEXT"
            -   uses: actions/checkout@v3
            -   uses: actions/setup-node@v3
                with:
                    node-version: 18
                    registry-url: 'https://registry.npmjs.org/'
            -   run: npm ci
            -   run: npm version ${{ github.event.release.tag_name }} --workspaces
            -   run: npm run build --workspaces
            -   run: npm publish --access public --workspaces
                env:
                    NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}

            -   name: Create bump version PR
                uses: peter-evans/create-pull-request@v4
                with:
                    commit-message: Bump version ${{ github.event.release.tag_name }}
                    committer: GitHub <noreply@github.com>
                    author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
                    branch: release-${{ github.event.release.tag_name }}
                    base: master
                    delete-branch: true
                    title: '[Release] ${{ github.event.release.tag_name }}'
                    assignees: lamo2k123
                    reviewers: lamo2k123
                    draft: false
#            -   run: git config --global user.email bot@easy-breezy.com
#            -   run: git config --global user.name bot
#            -   run: git checkout -b ${{ github.event.release.target_commitish }}
#            -   run: git pull origin ${{ github.event.release.target_commitish }}
#            -   run: git status
#            -   run: git add .
#            -   run: git commit -m "Bump version ${{ github.event.release.tag_name }}"
#            -   run: git push origin ${{ github.event.release.target_commitish }}
#            -   run: git push origin ${{ github.event.release.target_commitish }} --tags
